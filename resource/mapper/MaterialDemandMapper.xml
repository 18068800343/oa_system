<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.ldxx.dao.MaterialDemandDao" >
  <insert id="addMaterialDemand" parameterType="java.util.List">
  	insert into material_demand(mdl_id,material_name, material_code, buy_company, 
      price,num, provide_company,sure_material_mode, desc)
    values
    	<foreach collection="md" item="item" separator=",">
	    	(#{item.mdlId},#{item.materialName},#{item.materialCode},#{item.buyCompany},
	    	 #{item.price},#{item.num},#{item.provideCompany},#{item.sureMaterialMode},#{item.desc})
    	</foreach>
  </insert>
  
  <select id="selectMaterialDemandById" resultType="MaterialDemand">
  	select * from material_demand where mdl_id=#{id}
  </select>
</mapper>